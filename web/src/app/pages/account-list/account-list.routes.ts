import { Routes } from '@angular/router';
import { AccountListComponent } from './account-list.component';

export const ACCOUNT_LIST_ROUTES: Routes = [
  { path: '', component: AccountListComponent },
];
