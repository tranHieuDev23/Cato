import { Routes } from '@angular/router';
import { SubmissionListComponent } from './submission-list.component';

export const SUBMISSION_LIST_ROUTES: Routes = [
  { path: '', component: SubmissionListComponent },
];
